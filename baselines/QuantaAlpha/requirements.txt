# Core package dependencies.
#
# Keep this list lightweight and compatible with PyHealth's Python 3.12 stack.
# QuantaAlpha's original LLM/Qlib runtime dependencies are available through
# optional extras in pyproject.toml.

filelock
numpy>=1.24,<3.0
pandas>=1.5,<3.0
scikit-learn>=1.3
